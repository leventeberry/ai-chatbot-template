root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/goapi main.go"
  bin = "tmp/goapi"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "node_modules"]
  delay = 1000
  stop_on_error = true

[log]
  time = true
